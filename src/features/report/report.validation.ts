import z from "zod"

export class ReportValidation {
    static readonly PARAMS = {

    }
    static readonly QUERY = {
        getSales: z.object({
            
        })
    }
    static readonly BODY = {}
}